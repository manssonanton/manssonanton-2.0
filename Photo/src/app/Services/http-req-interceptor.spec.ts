import { HttpReqInterceptor } from './http-req-interceptor';

describe('HttpReqInterceptor', () => {
  it('should create an instance', () => {
    expect(new HttpReqInterceptor()).toBeTruthy();
  });
});
